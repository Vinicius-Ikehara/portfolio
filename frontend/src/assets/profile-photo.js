// Imagem de perfil em base64 como fallback
// Esta imagem será usada quando a URL da API não carregar
export const defaultProfilePhoto = '/profile-placeholder.svg'

// Para usar uma imagem real, substitua a URL acima pelo data URI da imagem
// Exemplo: export const defaultProfilePhoto = 'data:image/jpeg;base64,...'
